{
  "name": "gsdev-api",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^0.16.0"
  },
  "scripts": {
    "serve": "json-server --watch db.json"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
